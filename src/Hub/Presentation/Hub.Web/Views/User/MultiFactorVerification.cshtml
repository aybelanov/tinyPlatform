@model MultiFactorAuthenticationProviderModel

@{
   Layout = "_ColumnsOne";

   //title
   AppHtml.AddTitleParts(T("PageTitle.MultiFactorVerification").Text);
}

<div class="page login-page">
   <div class="page-title">
      <h2>@T("Account.Login.Welcome")</h2>
   </div>
   <div class="page-body">
      <div class="provider-verification">
         @(await Component.InvokeAsync(Model.ViewComponentName))
      </div>
   </div>
</div>